/**
 * 사이트 구분 메타데이터 — 정본은 엔티티(@/entities/sites/site)로 이전됨.
 * 기존 feature 공개 API 호환을 위해 재-export 한다.
 */

export {
  SITE_CATEGORIES,
  DEFAULT_SITE_CATEGORY,
  getSiteCategoryLabel,
  type SiteCategoryMeta,
} from '@/entities/sites/site'
