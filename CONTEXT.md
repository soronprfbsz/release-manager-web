# Release Manager

릴리즈/패치 산출물을 생성·배포하고, 이를 수행하는 사람들의 로그인 계정과 권한을 관리하는 사내 도구. 이 문서는 구현 세부가 아닌 **도메인 용어 사전**이다.

## Language

### 계정 / 인증

**Account**:
시스템 로그인 신원 레코드. 이메일(고유), 역할(Role), 상태(ACTIVE/INACTIVE/SUSPENDED), 비밀번호 해시를 가진다.
_Avoid_: User(레코드를 가리킬 때), Member — "User"는 그 계정을 쓰는 사람을 가리킬 때만 사용한다.

**Role**:
한 **Account**의 권한 등급. 값: `ADMIN`, `OPERATOR`, `DEVELOPER`, `USER` (그리고 가입 기본값 `GUEST`). 코드로 표현된 위계는 없으며 각 기능이 필요한 역할을 직접 검사한다.

**내 정보 (My Account)**:
로그인한 본인의 **Account** 프로필. 본인이 직접 보고 수정하는 화면.
_Avoid_: 프로필 설정, 마이페이지 — UI 라벨은 "내 정보"로 통일.

**계정 관리 (Account Management)**:
`ADMIN`/`OPERATOR`가 **모든 Account**를 조회·수정·삭제하는 관리 화면.

### 비밀번호 — 두 개념을 분리한다

**비밀번호 변경 (Change Password)**:
**본인**이 자신의 비밀번호를 새 값으로 바꾸는 행위. 본인이 **현재 비밀번호를 알고 있음**을 증명(현재 비밀번호 입력)해야 한다. "내 정보"에서 수행.
_Avoid_: 비밀번호 수정, 비밀번호 재설정 — "변경"으로 통일.

**비밀번호 초기화 (Reset Password)**:
**관리자(ADMIN/OPERATOR)**가 **다른 Account**의 비밀번호를 시스템이 만든 **임시 비밀번호**로 덮어쓰는 행위. 대상자의 현재 비밀번호를 모르는 상태에서 수행한다. "계정 관리"에서 수행.
_Avoid_: 비밀번호 재발급, 비밀번호 리셋 — "초기화"로 통일. **"변경"과 혼용 금지** (주체·전제·전달 방식이 다르다).

**임시 비밀번호 (Temporary Password)**:
**비밀번호 초기화** 시 시스템이 생성하는 1회성 랜덤 비밀번호. 평문은 저장되지 않고 초기화 응답에서 한 번만 노출되어, 운영자가 대상 User에게 **대역(out-of-band)으로 전달**한다.

**강제 비밀번호 변경 (Forced Password Change)**:
**임시 비밀번호**로 로그인한 Account가 다른 기능을 쓰기 전에 반드시 **비밀번호 변경**을 마치도록 강제하는 상태(`must_change_password`). 변경 완료 전까지 앱 사용이 차단된다.

## 예시 대화

> **운영자**: 부국증권 담당자가 비밀번호를 잊었대요. 제가 바꿔줄 수 있나요?
> **개발자**: 본인이 현재 비밀번호를 모르니 "변경"은 못 합니다. 운영자님이 "계정 관리"에서 그 **Account**를 **초기화**하면 **임시 비밀번호**가 한 번 화면에 뜨고, 그걸 담당자에게 전달하시면 됩니다.
> **운영자**: 그 임시 비밀번호로 계속 쓰면 되나요?
> **개발자**: 담당자가 로그인한 뒤 "내 정보"에서 본인이 직접 "변경"하도록 안내하세요. 변경은 현재(=임시) 비밀번호를 입력해야 진행됩니다.
